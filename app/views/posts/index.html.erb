<h1>Posts y Formulario</h1>

<table>
    <tr>
        <th>titulo</th>
        <th>imagen</th>
        <th>contenido</th>
        <th>creación</th>
    </tr>

<% @posts.each do |post| %>
    <tr>
        <td><%=post.title%></td>
        <td><%= image_tag post.image_url %></td>
        <td><%=post.content%></td>
        <td><%=post.created_at%></td>
    </tr>
<% end %>
</table>

<h2>Ingrese su post</h2>
<form class="form" method="get" action="posts/create">
    Titulo:<input name="titulo">
    Url Imagen:<input name="url_img">
    Contenido:<textarea name="contenido"></textarea>
    <input type="submit" value="Enviar">
</form>